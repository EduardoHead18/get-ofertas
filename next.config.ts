import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['imgs.search.brave.com','http2.mlstatic.com'], // Agrega aquí el dominio de la imagen
  },
};

export default nextConfig;
